# Minimal requirements for serverless deployment (faster cold start)
# Core FastAPI dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Web scraping (essential)
aiohttp>=3.9.0
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
httpx>=0.25.0

# Utilities (lightweight)
cachetools>=5.3.2
python-dateutil>=2.8.2

# Heavy dependencies (consider alternatives)
# yfinance>=0.2.28  # Heavy due to pandas/numpy (~50MB+ dependencies)

# Alternative lightweight packages for production:
# requests-cache>=0.9.0  # Instead of complex caching
# httpx>=0.25.0         # Already included, can replace requests+aiohttp
